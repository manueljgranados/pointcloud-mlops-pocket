services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    environment:
      - MODEL_DIR=/app/artifacts/models/current
    volumes:
      - ../artifacts:/app/artifacts
    ports:
      - "8000:8000"
